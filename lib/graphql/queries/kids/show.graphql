query GetEpisodesForShow($id: ID!) {
  show(id: $id) {
    title
    episodeCount
    seasons {
      items {
        episodes {
          items {
            id
            title
            description
            duration
            image
          }
        }
      }
    }
  }
}
